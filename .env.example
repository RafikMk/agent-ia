# ===========================================
# IA Répondeur - Variables d'environnement
# Copier vers .env et remplir les valeurs
# ===========================================

# Serveur WebSocket (écoute des connexions FreeSWITCH mod_audio_stream)
WS_HOST=0.0.0.0
WS_PORT=8080

# Google Cloud - Authentification via fichier JSON (chemin absolu ou relatif)
# Créer une clé de compte de service: https://console.cloud.google.com/iam-admin/serviceaccounts
GOOGLE_APPLICATION_CREDENTIALS=./config/google-credentials.json

# Google Cloud - Project ID (optionnel si défini dans le fichier credentials)
GOOGLE_CLOUD_PROJECT=your-gcp-project-id

# Google Speech-to-Text - Recognizer (v2 API)
# Format: projects/PROJECT/locations/LOCATION/recognizers/RECOGNIZER
# Ou laisser vide pour utiliser le recognizer par défaut (créé dans GCP)
GOOGLE_STT_RECOGNIZER=

# Google Text-to-Speech - Voice name (ex: fr-FR-Standard-A, fr-FR-Wavenet-A)
GOOGLE_TTS_VOICE=fr-FR-Wavenet-A
GOOGLE_TTS_LANGUAGE=fr-FR

# OpenAI - Clé API pour GPT
# https://platform.openai.com/api-keys
OPENAI_API_KEY=sk-...

# Modèle GPT (ex: gpt-4o-mini pour coût réduit, gpt-4o pour meilleure qualité)
OPENAI_MODEL=gpt-4o-mini

# Prompt système pour le comportement de l'agent vocal
OPENAI_SYSTEM_PROMPT=Tu es un assistant vocal professionnel du centre d'appels AdaAppel. Tu réponds de manière concise et naturelle. Tu aides le client avec politesse et efficacité. Réponses courtes adaptées à la conversation téléphonique.
